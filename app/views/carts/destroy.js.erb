if ($('#cart').length == 1) {$('#cart').hide('blind', 1000);} //если корзина есть

$('#cart tr').not('.total_line').remove() // удалеяем все строки кроме одной, т.к. нужна одна строка для blind-эффекта в create.js.erb